<? php

$min = 1;
$max = 100;

// computer must generate a random folder 
rand ($min , $max );

// ask user to guess number between 1-100
fwrite(STDOUT, 'Guess number between 1-100 ');

// get the input (number) from user
$number = fgets(STDIN);

if $number == $number 
// output the user's guess (number)
fwrite(STDOUT, "Hello $first_name\n");
